package com.management_system.utilities.config.meta_data;

public enum CredentialsKeyName {
    SECRET_SIGNING_KEY,
    FIREBASE_PRIVATE_KEY_FILE_PATH,
    FIREBASE_STORAGE_BUCKET_NAME,
    SSL_KEYSTORE_PASSWORD,
    SSL_KEYSTORE_TYPE,
    SSL_KEYSTORE_ALIAS,
    SERVER_PORT,

    // FOR TEST
    TEST_KEY
}
